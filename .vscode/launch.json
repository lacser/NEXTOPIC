{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "pwa-chrome",
            "request": "launch",
            "name": "Launch React project",
            "url": "http://localhost:3001",
            "webRoot": "${workspaceFolder}"
        },
    ]
}